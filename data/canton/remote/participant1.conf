canton {
  features {
    enable-preview-commands = yes
    enable-testing-commands = yes
  }
  remote-participants {
    remoteParticipant1 {
      admin-api {
        port = 10012
        address = 0.0.0.0 // is the default value if omitted
      }
      ledger-api {
        port = 10011
        address = 0.0.0.0 // is the default value if omitted
      }
    }
  }
}